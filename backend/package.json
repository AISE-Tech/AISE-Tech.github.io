{
  "name": "aise-tech-backend",
  "version": "1.0.0",
  "description": "Backend para AISE Technology con asistente IA usando Gemini",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node build.js --env=development && nodemon server.js",
    "obfuscate": "node obfuscate.js",
    "build": "node build.js --env=production",
    "build:dev": "node build.js --env=development",
    "build:prod": "node build.js --env=production",
    "start:prod": "node build.js --env=production && node server.js",
    "start:dev": "node build.js --env=development && nodemon server.js"
  },
  "author": "AISE Technology",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ws": "^8.15.1"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "nodemon": "^3.0.2"
  }
}
